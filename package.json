{
  "name": "porfolio",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:gh-pages": "GITHUB_PAGES=true vite build",
    "images:sync": "node scripts/sync-portfolio-images.js",
    "preview": "npm run build && vite preview",
    "preview:gh-pages": "npm run build:gh-pages && vite preview --base /Porfolio/"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  },
  "dependencies": {
    "@studio-freight/lenis": "^1.0.42",
    "gsap": "^3.15.0"
  }
}
